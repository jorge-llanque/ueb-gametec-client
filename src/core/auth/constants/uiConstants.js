export const UI = {
  AUTH: {
    LOGIN: {
      PROCESSING: 'Processing login',
    },
    ERROR: {
      INVALID_RETURN_URL: 'Invalid return url. The return url needs to have the same origin as the current page.',
    }
  }
}